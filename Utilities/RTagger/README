RTagger
Written by Fjölnir Ásgeirsson (fjolnir@gmail.com, http://ninjakitten.us)
======================================================================================
RTagger is a simple interface to libmp4v2 and taglib (both are provided as frameworks)
There's really not much to say that the following usage example doesn't explain:

NOTE: The required frameworks can't be hosted on code.google.com (license issue) so
      you must download them from http://ninjakitten.us/RTaggerFrameworks.zip
Example Usage
======================================================================================
id<RTaggerProtocol> tagger = [RTagger taggerWithPath:@"/path/to/mp3OrAAC.aac"];
// Get specific tags (For a full list of tags check RTaggerProtocol in RTagger.h)
NSLog(@"Title: %@", [tagger title]);
NSLog(@"Artist: %@", [tagger artist]);

// Dump the whole thing
// 	Get a dictionary containing all tracks the audio file contains
NSDictionary *info = [tagger info];
// 	Enumerate through each of the tags and print them out
NSEnumerator *keyEnumerator = [[info allKeys] objectEnumerator];
NSString *key, *value;
while(key = [keyEnumerator nextObject])
{
	value = [info objectForKey:key];
	NSLog(@"%@ = %@", key, value);
}