{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {
                addNewVersion = id; 
                cancelVersionInfoSheet = id; 
                chooseEnclosure = id; 
                closeProjectInfoSheet = id; 
                closeServerConfigSheet = id; 
                okVersionInfoSheet = id; 
                previewReleaseNotes = id; 
                saveXML = id; 
                showConfigSheet = id; 
                showProjectInfoSheet = id; 
            }; 
            CLASS = MyDocument; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                fileDropImageView = FileDropImageView; 
                mainWindow = NSWindow; 
                previewMatrix = NSMatrix; 
                projectInfoSheet = NSWindow; 
                serverConfigController = ServerConfigController; 
                statusTextField = NSTextField; 
                urlPreviewPanel = NSPanel; 
                urlTextField = NSTextField; 
                versionArrayController = NSArrayController; 
                versionInfoController = NSObjectController; 
                versionInfoSheet = NSWindow; 
                versionListTableView = NSTableView; 
                webView = WebView; 
                xmlProgressIndicator = NSProgressIndicator; 
            }; 
            SUPERCLASS = NSDocument; 
        }, 
        {
            CLASS = ServerConfigController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                appcastURL = NSTextField; 
                documentController = MyDocument; 
                dotMacAccountStatus = NSTextField; 
                dotMacPasswordTextField = NSTextField; 
                dotMacUserNameTextField = NSTextField; 
                enclosureURL = NSTextField; 
                serverConfigSheet = NSWindow; 
            }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}